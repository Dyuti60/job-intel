# Copy this file to D:\ASSAM_JOB_DATA\config\runtime.env on the trusted runner.
# Keep the real file outside the repository and restrict it to administrators and
# the Windows account running the GitHub Actions runner service.
AJI_DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/DATABASE
